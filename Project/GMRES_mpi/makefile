# ======================================================
# MPI CFD MAKEFILE (FIXED)
# ======================================================

CC      = mpicc
CFLAGS  = -O2 -Wall -std=c99
LIBS    = -lm

# ======================================================
# SOURCES
# ======================================================
SRCS = \
    main_mpi.c \
    initialize_mpi.c \
    io_mpi.c \
    mesh_mpi.c \
    operator_mpi.c \
    solver_mpi.c \
    utils_mpi.c \
    vtk_mpi.c

OBJS = $(SRCS:.c=.o)

TARGET = solve_mpi

# ======================================================
# BUILD ALL
# ======================================================
all: $(TARGET)

# ======================================================
# LINKING
# ======================================================
$(TARGET): $(OBJS)
	$(CC) $(CFLAGS) -o $@ $^ $(LIBS)

# ======================================================
# COMPILATION
# ======================================================
%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

# ======================================================
# CLEAN
# ======================================================
clean:
	rm -f $(OBJS) $(TARGET)
	rm -rf vtk_mpi

clean-data:
	rm -rf vtk_mpi

# ======================================================
# RUN
# ======================================================
run:
	mpirun -np 4 ./$(TARGET)